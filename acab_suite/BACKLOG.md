# BACKLOG — Suite ACAB (pendientes no planificados)

Convención: este documento recoge correcciones, mejoras e ideas AÚN NO planificadas.
Cuando un ítem se aborda, se convierte en sesión/runbook y su línea pasa a ✅ con
fecha (o se mueve al tablón de README.md si genera verificaciones). El estado de
los runbooks y las verificaciones de control viven en README.md (tablón), no aquí.

Prioridad: A (hacer pronto, valor alto o corrige fricción real) · B (cuando toque) ·
C (idea registrada, decidir más adelante). Esfuerzo: S (<1 h) · M (una sesión) ·
L (varias sesiones).

---

## Correcciones y limpieza (de la revisión 2026-07-13)

| # | Comp. | Ítem | Prio/Esf |
|---|---|---|---|
| C1 | docs | Retoques CLAUDE.md: notas recíprocas en COLLAPS (parser y writer → coll_writer.py); padre a monorepo con prefijos de commit (`inp-conf:`/`analyzer:`/`collaps:`/`suite:`) y COL.inp→COLL.inp; restaurar en INP configurator las frases de `_write_inp5`/"nunca duplicar formato" y de docs/ como fuente de verdad; en analyzer, mover el párrafo de la pestaña "Actividad por Isótopo" del bullet de optim_utils.js al de app.js. **✅ (2026-07-14):** ya aplicado en el commit `7c23288` (el mismo que creó este backlog); verificados los 4 puntos contra el estado actual de los ficheros, sin cambios pendientes. | A / S |
| C2 | git | Monorepo: `.gitignore` de raíz correcto (venvs, `acab_suite/logs/`, salidas de simulación), `git rm --cached` de lo trackeado indebidamente, commit adoptando la convención de prefijos. **✅ (2026-07-14):** commit `suite:`. Fixtures de tests oro verificados como no excluidos; las salidas dentro de `examples/Simulation/Reference simulation/Simulation v.0/` se dejan trackeadas a propósito (decisión pendiente en C4). | A / S |
| C3 | analyzer | i18n: clave `optim.type_spectrum` sin traducir + errata "Barrido de expectro" → "espectro". **✅ (2026-07-14):** commit `analyzer:`, añadida la clave en es.json/en.json. La errata no se encontró en ningún sitio del repo (grep + historial completo). | A / S |
| C4 | inp-conf | Excluir/limpiar salidas viejas al copiar la carpeta base del barrido (FLUX.inf, XSECTION.dat, fort.6, run.log…): un fichero muerto con datos de OTRO espectro en la carpeta de una sim es una trampa de trazabilidad (visto en el control MURR). Alternativa mínima: documentar que las salidas de `collaps/` se regeneran. **✅ (2026-07-14):** commit `inp-conf:`. Exclusión asimétrica según `sweep_type`: espectral excluye salidas de ACAB Y de COLLAPS (se regeneran por sim); flujo/masa/temporal excluye solo las de ACAB (XSECTION.dat/FLUX.inf se conservan a propósito, espectro compartido por diseño). Lo excluido queda anotado en `sweep_manifest.json` (`excluded_base_files`). Tests oro nuevos en `tools/test_sweep_endpoint.py` (`BaseFolderExclusionTests`). | A / S-M |
| C5 | inp-conf | Baseline con 2 tests en rojo: rutas rotas tras la reorganización de `examples/` en subcarpetas (detectado en la sesión C4) + CLAUDE.md con rutas/comandos desactualizados por la misma reorg. Actualizar tests y CLAUDE.md al layout actual; criterio de cierre: suite completa en verde SIN fallos "conocidos". **✅ (2026-07-14):** commit `inp-conf:`. Los 2 fallos eran los comandos documentados en CLAUDE.md (`regression_roundtrip.py`/`test_parser_robustness.py` con rutas `examples/exp*.inp.5` de antes de la reorg); ningún test bajo `tools/` tenía rutas hardcodeadas. Corregido a `examples/Inp5/exp*.inp.5`; suite completa (Python + node) verificada en verde. | A / S |
| C6 | analyzer | leer_fort6_irradiacion/enfriamiento no reconocen isótopos de elementos de símbolo de una letra («O 16», tokens separados) — detectado en F9 Fase 1; sin impacto en el flujo I-131. Alinear con leer_concentraciones_iniciales cuando toque. | C / S |

## Mejoras de usabilidad (notas de pruebas 2026-07-13)

| # | Comp. | Ítem | Valoración | Prio/Esf |
|---|---|---|---|---|
| U1 | inp-conf | "Abrir en Fort Analyzer" tras el barrido: pasar de pestaña nueva a MISMA pestaña (coherencia con el banner de la suite; es el único `_blank` del sistema). Ctrl+clic sigue dando pestaña nueva de forma nativa. Coste asumido: al volver atrás se pierde el estado en memoria de la pestaña Barrido (el trabajo ya está hecho y la cola vive en servidor). **✅ (2026-07-15):** commit `inp-conf:`. Retirado `target="_blank" rel="noopener"` de `btn-sweep-run-open-analyzer` (único `_blank` del sistema); el resto del comportamiento (Ctrl+clic → pestaña nueva) es nativo del navegador, no requería cambios. Verificado en navegador real (Playwright): clic normal navega en la misma pestaña sin abrir popup, Ctrl+clic sigue abriendo pestaña nueva. | Aprobada | A / S |
| U2 | collaps + inp-conf | Guardar en carpeta en vez de descargar: botón "Guardar en carpeta…" (diálogo browse-folder patrón analyzer, con fallback de ruta manual) que escribe COLL.inp/inp.5 en la carpeta elegida; recordar la última. "Descargar" se mantiene como opción secundaria (patrón dual del editor de figuras YAML). Aplicar SIMÉTRICAMENTE en ambos configuradores. **✅ (2026-07-15):** commits `collaps:` e `inp-conf:`. Botón primario "Guardar en carpeta…" en la barra de navegación de ambas apps (`POST /api/save-to-folder`, escribe COLL.inp/inp.5); diálogo de carpeta vía `/api/browse-folder` (tkinter en subprocess, ya existente en inp-conf/analyzer, añadido en collaps) con fallback de ruta manual. Confirmación explícita antes de sobrescribir (409 + reintento con `overwrite:true`, patrón de figuras.yaml del analyzer). Última carpeta recordada en `localStorage` (por app) y ofrecida como valor inicial en el siguiente guardado. "Descargar" (antes "Guardar como…") sin cambios de lógica, relegada a opción secundaria — en inp-conf conserva su flujo `showSaveFilePicker` en Chromium. Tests nuevos `tools/test_save_to_folder.py` en ambos repos (construcción de ruta, 400/422, 409+overwrite); suites completas (Python + node en inp-conf) en verde antes y después. Verificado en navegador real (Playwright) en ambas apps: simetría confirmada — mismos textos base y misma jerarquía visual de botones. | Aprobada | A / M |
| U3 | collaps | Al ejecutar COLLAPS, prefijar el workdir con la carpeta del último guardado de COLL.inp (editable, nunca oculto); sin guardado previo, diálogo de carpeta. Depende de U2. Aplicar el mismo criterio en el INP configurator con el inp.5. **✅ (2026-07-15):** commits `collaps:` e `inp-conf:` (junto con U2). El workdir de ejecución se precarga con la carpeta del último "Guardar en carpeta…" (prioridad sobre el `default_workdir` de la última ejecución); sin guardado previo, cae al comportamiento de siempre. Campo siempre visible y editable; se le añadió su propio botón de diálogo de carpeta (mismo patrón que U2). Verificado en navegador real (Playwright) en ambas apps. | Aprobada | A / S (tras U2) |
| U4 | analyzer | Pestaña Optimización, barridos espectrales: leyenda ilegible (volcado de parámetros por serie). Mostrar una sola serie con el NOMBRE del espectro como etiqueta/hover. **✅ (2026-07-15):** commit `analyzer:`. `optim_utils.js`: `isSpectrumSweep`/`spectrumRowLabel` (el NOMBRE del espectro, `params.espectro`; degrada al folder si un manifest viejo no lo trae, nunca a un volcado de parámetros — criterio compartido con U6). `renderOptimizacion` detecta el barrido espectral y delega en `renderOptimizacionSpectrum`: una SOLA serie (barras, eje X categórico con el nombre del espectro) por métrica; los otros 3 tipos de barrido no cambian. i18n es/en completa. Tests nuevos en `tools/test_optim_utils.js`; suite completa (Python + node) en verde antes y después. Verificado en navegador real (Playwright) con el barrido REAL de 9 reactores de investigación (`Barrido 5`, fuera del repo): gráfica de barras legible con los 9 nombres de espectro en el eje X, sin volcado de parámetros; regresión confirmada con un barrido de flujo (comportamiento sin cambios). | Aprobada | B / S |
| U5 | inp-conf | Barrido de flujo, modo "Flujo total objetivo": placeholder y etiqueta heredados del modo XNORM inducen a error (introducir factores como si
fueran flujos → XNORM ~1e-14 sin aviso). (a) Placeholder dinámico por modo, derivado del φ_base real del fichero (×0.5/×1/×2); (b) etiqueta con unidad "Valores de flujo total [n/cm²·s]"; (c) guardarraíl en previsualización: aviso si el XNORM resultante sale de [1e-3, 1e3] (y el simétrico en modo factores).
**✅ (2026-07-15):** commit `inp-conf:`. Etiqueta y placeholder del campo de valores ahora dependen del modo (`fluxValuesPlaceholder` en `sweep_utils.js`); en modo flujo, sin fichero base cargado o φ_base ≤ 0 se usa un texto genérico con la unidad, nunca el placeholder de factores. Guardarraíl (`fluxSweepGuardrail`) en la previsualización, no bloqueante, con el mismo umbral [1e-3, 1e3] en ambas direcciones. i18n es/en completa. Tests nuevos en `tools/test_sweep_utils.js`; suite completa (Python + node) en verde antes y después. Verificado en navegador real (Playwright) con el fixture `exp1.inp.5` (φ_base=6.5000e+13): cambio de modo actualiza placeholder/etiqueta, aviso se dispara en ambas direcciones con el mensaje correcto y no se dispara con valores limpios.
| Aprobada | A / S |
| U4b | analyzer | Restaurar eje X numérico en Optimización para barridos espectrales con etiquetado por nombre de espectro. U4 arregló la leyenda (barras por nombre de espectro) pero eliminó el selector de eje X, y la figura central de la memoria es A_pico vs. frac_termica. **✅ (2026-07-16):** commit `analyzer:`. `optim_utils.js`: `SPECTRUM_FRAC_KEYS`/`spectrumNumericKeys` (fracciones espectrales presentes en el manifest, en orden fijo, nunca `n_grupos`) y `spectrumTextPositions` (desplazamiento simple arriba/abajo de etiquetas para puntos próximos en X). `renderOptimizacionSpectrum` restaura el selector "Parámetro (eje X)": opción categórica "Espectro" (comportamiento de barras de U4 sin cambios) o una fracción numérica → `_renderSpectrumScatterChart`, una SOLA serie de dispersión con el nombre del espectro como etiqueta de texto junto a cada punto (nunca leyenda por combinación de parámetros). Manifest viejo sin fracciones → opción deshabilitada con nota, no rompe; otros 3 tipos de barrido intactos. i18n es/en completa. Tests nuevos en `tools/test_optim_utils.js`; suite completa (Python + node) en verde antes y después. Sin acceso a navegador real en esta sesión: no se pudo verificar visualmente con el barrido real de 9 reactores (fuera del repo); pendiente de verificación manual. | Aprobada | A / S |
| U6 | inp-conf | Cargar un barrido ya generado para CONSULTARLO (solo lectura): hoy se puede cargar para ejecutar pero no para ver qué lo compone. Vista resumen desde el manifest: tipo (los 4), parámetro barrido y valor por simulación, base, ficheros excluidos (C4), y estado de ejecución por sim si hay batch_results.json. Unificar con el flujo de carga existente: cargar muestra el contenido, y ejecutar pasa a ser una acción sobre lo cargado. Editar queda explícitamente fuera (editar = regenerar). **✅ (2026-07-15):** commit `inp-conf:`. `sweep_manifest_view.py`: `build_manifest_view(root)` lee `sweep_manifest.json` (+ `batch_results.json` si existe) y añade por simulación `value_label` (el NOMBRE del espectro en el barrido espectral — criterio compartido con U4 del analyzer — o `None` si no se puede derivar) y `fort6_exists`; tolera manifests PRE-C4 sin `excluded_base_files`. Nuevo `GET /api/sweep/manifest`. La carga existente (patrón browse-folder de U2/U3) se unificó: siempre muestra esta vista; "Ejecutar" pasa a ser una acción sobre el barrido cargado (reutiliza `startSweepBatchRun`/panel de la Fase R4). i18n es/en completa. Tests nuevos en `tools/test_sweep_manifest_view.py` (los 4 tipos, un manifest PRE-C4 congelado, corrupto/ausente, con/sin `batch_results.json`); suite completa (Python + node) en verde antes y después. Verificado en navegador real (Playwright): un barrido de cada tipo (uno ejecutado, dos sin ejecutar, uno PRE-C4) y "Ejecutar barrido" desde la vista cargada disparando correctamente `/api/run/batch`. | Aprobada | A / M |
| U7 | inp-conf | Barrido temporal con historiales multi-tramo: sustituir la fila plana (t_irr/pasos/t_cool/pasos) por el editor de tramos completo del historial manual, reutilizado por simulación (acordeón de tarjetas con resumen, duplicar, sembrado desde base). Semántica idéntica al editor manual: tiempo FINAL acumulado por tramo. **✅ (2026-07-23):** commit `inp-conf:`. `static/js/b78_editor.js` nuevo: el editor de tramos (IUNIT/IOUT/IPLOT + tablas irr/cool) se extrajo del generador manual a componente reutilizable parametrizado por prefijo de ids, sin referenciar nunca `appState` (los efectos laterales — dirty flag, resync de tarjeta — quedan en manos del `onChange` de cada llamador). `app.js` (pestaña manual, prefijo `b78`) y `sweep.js` (una instancia por tarjeta, prefijo `sweep-time-<id>`) llaman al MISMO código — cero divergencia de validaciones por construcción. `sweep_utils.js`: `buildTimePatches` reescrita para historiales completos por tarjeta (`{fasesIrr, fasesCool, iunit, iout, iplot}`, sin el fallback "campo vacío conserva la base"); `params` sigue exponiendo `t_irr_fin`/`t_cool_fin` escalares (el analyzer detecta claves numéricas genéricamente, cero cambios) más `historial_irr`/`historial_cool` completos para trazabilidad en el manifest. Nuevas funciones puras `summarizeFases` (resumen de tarjeta), `insertDuplicate` (clon+inserción genérico) y `uniqueSuffix` (consolida la desambiguación ya usada por el barrido espectral, reutilizada también para el temporal: dos tarjetas con el mismo `t_irr_fin` pero historial distinto ya no colisionan de carpeta). `sweep.js`: acordeón Bootstrap con `_timeSimCards` como estado único; tarjeta 1 sembrada desde `baseFases()` (colapsa la malla del fichero base a un tramo por fase — limitación preexistente del generador manual, no nueva de U7) con aviso visible en la propia tarjeta (`sweep.time_seeded_hint`, texto explícito sobre la malla colapsada). `sweep_manifest_view.py` sin cambios (`_sim_value_label` solo lee los escalares; manifests viejos Y nuevos funcionan igual, verificado con fixture de cada forma). `sweep_writer.py::_manifest_csv`: los valores no escalares de `params` (el historial multi-tramo) se vuelcan como JSON válido (`json.dumps`) en vez de repr de Python — el CSV es entregable de trazabilidad; de paso, corregido un bug preexistente e inadvertido de doble salto de línea entre filas (`csv.writer` + `Path.write_text` en Windows duplicaba `\r\n`), nunca detectado porque ningún test parseaba el CSV fila a fila hasta el caso oro de esta sesión. i18n es/en completa (incluye singular/plural del resumen de tarjeta y la migración de las cadenas de ayuda de tramos, antes hardcodeadas en el HTML de la pestaña manual, a `b78.irr_hint_html`/`cool_hint_html`). Tests nuevos: `tools/test_sweep_utils.js` (regresión 1 tramo/fase, multi-tramo, equivalencia estructural `buildTimePatches`≡`buildBlocks78`, `summarizeFases`, `insertDuplicate`, `uniqueSuffix` incluido el caso de colisión de sufijo con historiales distintos); `tools/test_sweep_endpoint.py::test_time_sweep_byte_identical_manual_vs_sweep_path` (caso oro nuevo: inp.5 BYTE-IDÉNTICO entre el camino manual `_write_inp5` directo y el camino del barrido vía `/api/sweep`, más el historial y el CSV JSON verificados — refresco automatizado de "Control de malla", ver README); `tools/test_sweep_manifest_view.py` con un fixture de la forma NUEVA (confirma que el manifest con `historial_irr/cool` da el mismo `value_label` que la forma plana vieja). Suite completa (Python + node, 709 tests) en verde antes y después. Verificado en navegador real (Playwright, headless vía `npx playwright` vinculado a una instancia propia en un puerto separado para no interferir con un proceso ya en marcha): fichero base cargado → tarjeta sembrada con aviso visible → "Añadir simulación" → tramo añadido y editado en la tarjeta nueva → duplicar → eliminar la sembrada → previsualizar (dos tarjetas con igual `t_irr_fin` y sufijos desambiguados `Tirr050.0h`/`Tirr050.0h_2`) → generar → cargar el resultado en la vista U6, sin errores de consola en ningún paso. Dos defectos reales encontrados y corregidos durante esta verificación: pluralización "1 tramos" → "1 tramo" en el resumen de tarjeta, y `paramsLabel` de la previsualización del barrido (genérica, no solo temporal) mostraba `[object Object]` para los historiales — se filtran los valores no escalares de la etiqueta compacta. | Aprobada | A / M |

## Mejoras científicas

| # | Comp. | Ítem | Valoración | Prio/Esf |
|---|---|---|---|---|
| F1 | analyzer | **Pureza como serie temporal P(t) durante el enfriamiento**, desde el fin de irradiación: gráfica con línea de umbral 99.9 % (requisito validado con el tutor) y marcador del instante de cruce ("tiempo mínimo de enfriamiento para calidad farmacéutica"). Emparejar con A(t) del ¹³¹I → ventana de administración (pureza alcanzada vs actividad restante). Las impurezas de yodo decaen más rápido que el ¹³¹I ⇒ P(t) crece tras el pico. Datos por timestep ya parseados: extender `calcular_pureza` de escalar a serie. Absorbe el badge de umbral pendiente. **✅ (2026-07-14):** ejecutado fase a fase (`RUNBOOK_F1_pureza_temporal.md`), 4 commits `analyzer:`. `calcular_pureza` ya estaba en base actividad (verificado en Fase 1, no hizo falta parar). `calcular_pureza_serie` nueva (P(t) en todo el enfriamiento, t_cruce con interpolación log-lineal + bisección, casos borde ya-alcanzado/nunca-alcanzado, aviso de no monotonía, ventana de administración), expuesta en `/api/isotopo_report` sin tocar el endpoint (ya serializaba `informe` completo). Gráfica de dos paneles apilados (P(t) arriba con umbral y marcador de cruce, A(¹³¹I,t) abajo) en `_renderPurezaSerieChart`, utilidades puras en `pureza_time_utils.js` nuevo con tests node. Verificado a mano: 3 timesteps de ref_sim contra el fort.6 + casos borde sintéticos con solución analítica cerrada; visual en navegador real (Playwright) con el caso oro, un barrido de 2 sims y los dos casos borde inyectados. Badge escalar "P = {v} %" retirado de la UI (absorbido por la gráfica). Suite completa (Python + node) en verde. | Aprobada — la de mayor valor para la memoria | A / M |
| F2 | analyzer | Actividad específica del yodo: A(¹³¹I)/masa total de yodo, con I-127 estable e I-129 de vida larga como diluyentes (concepto del tutor, 2026-07-09; sección de átomos ya en fort.6). **✅ (2026-07-16):** commit `analyzer:`. `calcular_actividad_especifica_yodo_serie` (misma clave `metricas`, campo `actividad_especifica_yodo_serie`): A_esp(t) = A(objetivo,t)/masa_total_yodo(t) en MBq/g, mismo dominio temporal que `pureza_serie` (F1). Masa de yodo = suma de TODOS los isótopos de yodo del fort.6: radiactivos con serie de enfriamiento vía N(t)=A(t)/λ (exacto, incluye alimentación de precursores durante el enfriamiento); estables (I-127) vía el último punto de la irradiación mantenido constante (única aproximación, documentada — el fort.6 no da población de especies estables en el enfriamiento). Átomos→gramos con el número másico como masa molar (error <0.1 %); `CONCENTRATIONS(GRAM)` nunca sirve aquí (solo cubre los elementos de partida del target). `analizar_carpeta` guarda ahora también `datos_irr_atomos` (la conversión a Bq pierde la población de isótopos estables). Solo aplica a isótopos de yodo; `t_destacado_h`/`valor_destacado_MBq_g` resaltan el valor en el t_cruce de pureza ya resuelto. Gráfica de un panel (`_renderActividadEspecificaYodoChart`, sin umbral ni semáforos — fuera de alcance) bajo P(t) en la pestaña Informe Isótopo; nueva variable Y `a_esp_yodo` seleccionable en Optimización para comparar entre espectros del barrido. i18n es/en completa. Tests nuevos en `test_metricas.py` (caso oro de ref_sim comprobado a mano: A_esp(t=0)=4505514996.653325 MBq/g; casos borde "solo I131" —invariante analítico frente al isótopo puro— y "sin I127 estable"), `test_api.py`, `test_optim_utils.js`; suite completa (Python + node) en verde antes y después. Sin acceso a navegador real en esta sesión: no se pudo verificar visualmente la gráfica ni el selector de Optimización; pendiente de verificación manual. **F2b ✅ (2026-07-21):** commit `analyzer:`. Bug confirmado y corregido: I-129 recuperaba N(t) vía A(t)/λ igual que cualquier radiactivo, pero su λ es tan diminuta (T½≈1.57e7 y) que amplifica el redondeo de imprenta del fort.6 en una masa sin precisión fiable (irrelevante en el pulso corto de ref_sim, pero dominante en irradiaciones largas donde Te127/Te129 alimentan I127/I129 durante horas). `IODINE_ESTABLE_O_VIDA_LARGA` (I127, I129) ahora se lee UNA VEZ de la tabla NUMBER OF ATOMS y se mantiene constante todo el enfriamiento; el resto de yodos radiactivos siguen vía N(t)=A(t)/λ por timestep. Nuevo valor oro en ref_sim: A_esp(t=0)=4505547272.634922 MBq/g (apenas cambia frente al anterior: en t=0, A=λN es una identidad exacta, así que tabla de átomos y A(0)/λ coinciden salvo el redondeo de imprenta de cada tabla — la divergencia real solo aparece en t>0 e irradiaciones largas). Tests nuevos: `test_actividad_especifica_yodo_i129_congelado_vs_creciente` (escenario sintético que exagera el crecimiento de A(I129,t) en enfriamiento, demuestra que la masa usa el valor congelado) y `test_actividad_especifica_yodo_techo_fisico` (A_esp(t) ≤ techo físico sin portador λ(I131)·N_A/masa(I131)≈4.5967E+09 MBq/g, en TODO t y toda simulación — ref_sim completo + sintéticos). Nota de procedencia: esta firma está anclada a `tests/fixtures/ref_sim` (simulación v.5 "info thesis"); el fort.6 v.7 usado en el diagnóstico manual original tiene una población de I127/I129 muy superior (I131≈4.017E+07, I129≈1.274E+07, I127≈2.378E+06 átomos) — son simulaciones distintas, no comparables directamente sin releer la tabla NUMBER OF ATOMS de cada una. Suite completa (Python + node) en verde antes y después. | Aprobada | B / M |
| F3 | inp-conf | Modo geométrico opcional (rampa ×2) en el generador de mallas temporales, junto al lineal actual — recomendación del manual para transitorios rápidos al inicio de irradiaciones largas. Comentar con el tutor si algún caso lo pide. | Registrada | C / M |
| F4 | inp-conf | Asistente manual FLUX.inf → Bloque #3 (leer REAL TOTAL FLUX de un FLUX.inf y volcarlo al inp.5): útil para espectros analíticos con magnitud física, FUERA de los barridos (en barridos el Bloque #3 es fijo por diseño, decisión D1). | Registrada | C / S |
| F5 | analyzer | Badge "¿compensa seguir irradiando?" es artefacto en pulsos cortos con crecimiento por precursor (bases distintas pico-enfriamiento vs curva de irradiación). Documentado como limitación; decidir arreglo (ambos términos sobre la curva de irradiación) cuando se analice un caso de irradiación larga. | Registrada | C / S-M |
| F6 | analyzer | Métricas de desviación también contra series computacional_referencia (réplica vs modelo publicado, la comparación clave del Conjunto 1) + selector de simulación objetivo cuando hay varias cargadas (hoy: solo la activa/primera). **✅ (2026-07-22):** commit `analyzer:`. `reference_data.js`: `seriesForMetrics(series, iso)` (TODAS las series del isótopo entran en la tabla de desviación, sea su tipo `experimental` o `computacional_referencia` — antes solo la experimental; la distinción huecos/rellenos de la gráfica no cambia) y `resolveTargetSimName(simNames, requestedName)` (con varias simulaciones cargadas, decide contra cuál se interpolan TODAS las tablas — respeta la solicitada si sigue disponible, si no cae a la primera). `app.js`: `renderRefDataMetrics` genera una tabla por serie con el tipo visible en la cabecera ("Desviación de {label} [{tipo}] vs. {sim}"); desplegable `#refdata-target-sim` (`_state.refMetricsTargetSim`) visible solo con >1 simulación, junto a las tablas — `s.refSimName` (elegida al importar cada serie) queda solo para la conversión de unidad, ya no para las métricas. Cabecera de tabla y CSV exportado muestran tipo + simulación objetivo. La gráfica no cambia (sigue mostrando todas las curvas). i18n es/en completa. `docs/SPEC_csv_datos_referencia.md` actualizado (ambos tipos entran en métricas, selector documentado). Tests nuevos en `test_reference_data.py`/`.js` (`seriesForMetrics` con las series fig6/exp4 —una de cada tipo— del cuarto experimento, ya fixtures de la Fase 4; `resolveTargetSimName` con nombres de simulación sintéticos); suite completa (Python + node, 689 tests) en verde antes y después. Verificado en navegador real (Playwright): dos simulaciones scratch + las dos series fig6/exp4 cargadas, desplegable visible, cambio de objetivo actualiza coherentemente las cabeceras y las 4 cifras de sesgo/desviación máxima (comprobado con precisión completa, no solo el redondeo a 2 decimales de la UI) en ambas tablas; con una sola simulación cargada (ref_sim) el desplegable no aparece y la única tabla se muestra igualmente. | A / S-M |
| F7 | inp-conf (+analyzer verif.) | Generación de Blocks #7/#8 sin compactación: tarjetas alineadas por fase (irradiación separada del enfriamiento), formato solicitado por el tutor. Misma malla, solo reagrupado. Consecuencia: fort.6 pasa a 3+ TIME SETs — verificar parser del analyzer y re-anclar controles. **✅ (2026-07-23):** 2 commits (`inp-conf:`, `analyzer:`). `buildBlocks78` (`sweep_utils.js`) trocea irradiación y enfriamiento POR SEPARADO en grupos de <=10 tiempos (antes: lista global única troceada de 10 en 10, mezclando fases dentro de una misma tarjeta); NGO/MSUB siguen encadenando globalmente (MSUB = MOUT de la tarjeta anterior, NGO=0 solo en la última). Caso oro literal del tutor (irr 2.778e-3h/1 paso + cool 4.5h/18 pasos) verificado byte a byte contra `_write_inp5`. Tests nuevos en `test_sweep_utils.js`: caso oro, fase de irradiación >10 pasos (2 tarjetas puras de irr), solo-irradiación, solo-enfriamiento, y guardarraíl "ninguna tarjeta mezcla fases" sobre el caso multi-tramo de U7; generador manual y barrido siguen heredando el mismo cálculo sin cambios propios (arquitectura U7). Verificación end-to-end: regenerado el inp.5 de la ref_sim con el nuevo formato y ejecutado `acab.exe` real → fort.6 con 3 TIME SETs (antes 2). Esto expuso un bug en `leer_fort6_enfriamiento` (analyzer): el token RESTART se trataba siempre como "excluir", pero con F7 la transición irr→cool puede caer justo en un límite de tarjeta y aparecer como RESTART en vez de SHUTDOWN, perdiendo el t=0 real. Arreglado mapeando RESTART a 0.0 igual que SHUTDOWN, dejando que la deduplicación por "tiempo ya visto" decida si es el punto nuevo o un duplicado de continuación (regresión verificada: ref_sim con 2 TIME SETs sigue igual). Fixture nuevo `tests/fixtures/ref_sim_f7/` (misma física que `ref_sim`, procedencia documentada) + test oro en `test_fort_analyzer.py`: los 19 timesteps fusionan sin duplicados y reproducen EXACTAMENTE los valores oro de `ref_sim` (A_pico=1.6500e4 Bq/cm³, t_pico=3.753h, t_cruce=0, A_esp(0)=4505547272.63 MBq/g). Suite completa (738 tests, `run_all_tests.ps1`) en verde antes y después. **Hallazgo adicional (mismo día):** `leer_fort6_irradiacion` tenía el mismo problema pero al revés — con una fase de irradiación que ocupa >1 tarjeta (formato F7) solo leía la PRIMERA tabla NUMBER OF ATOMS y paraba en el literal `"2. TIME SET"`, cortando la curva de irradiación en el final de la primera tarjeta. Reescrita con la misma fusión multi-bloque que `leer_fort6_enfriamiento` (RESTART aquí SIEMPRE es un duplicado de continuación, nunca genuino, porque el reloj de irradiación no se reinicia entre tarjetas de la misma fase — a diferencia del RESTART de enfriamiento). Fixture nuevo `tests/fixtures/ref_sim_f7_irr2sets/` (irradiación en 2 tarjetas de 10 pasos, 0.25→5.00h) + 2 tests oro: 21 timesteps sin duplicados y estrictamente crecientes, I131 en el empalme de tarjetas (t=2.50h) idéntico al valor bajo RESTART de la tarjeta 2 (1.141e13 át/cm³, verificado a mano). Verificación visual (Playwright): la curva de irradiación es continua 0→5h sin salto en el límite de tarjeta, empalma con el enfriamiento en la línea de EOI, y la curva de saturación teórica se extiende a todo el tramo. Suite completa (753 tests) en verde antes y después. | Aprobada | A / M |
| F8 | inp-conf | Sincronizar ITSO (Bloque #13, vector [NOTTS]) con el historial temporal, igual que NOTTS: al generar historial (manual o barrido), redimensionar a NOTTS con valor 1 por set (un 0 amputa ese set del fort.6 en silencio). Bug hermano: buildTimePatches parchea block11.NOTTS pero no block13.ITSO. Dirección única historial→códigos. **✅ (2026-07-23):** commit `inp-conf:`. `buildTimePatches` (`sweep_utils.js`) añade `block13.ITSO` (vector de `NOTTS` unos) al patch junto a `block11.NOTTS` — el bug hermano: un barrido con nº de tarjetas distinto de la base generaba antes un ITSO con la longitud de la BASE, disparando V17e (o, sin la validación, un inp.5 con Block #13 desalineado del resto de tokens). El generador manual (`generarB78` en `app.js`) sincroniza igual el campo `b13-ITSO` al redimensionar `NOTTS`; si el ITSO previo no era "todo unos" (el usuario había suprimido la salida de algún set a mano), un aviso no-modal (`showToast`, i18n es/en `b78.itso_resized_hint`) lo informa en vez de pisarlo en silencio. Semántica documentada en ambos sitios: NOTTS = nº de tarjetas, ITSO\[NOTTS\] = flag de salida por time set (manual ACAB 2008, Block #13; la salida por intervalo exige además IOUT=1 en la tarjeta). Tests nuevos: `test_sweep_utils.js` (ITSO en el patch, longitud correcta en el caso de 1 tramo/fase y en el multi-tramo); `test_validate_all.js` (un barrido con nº de tarjetas != la base de este fichero valida limpio tras el fix — antes disparaba V17e). `test_sweep_endpoint.py`: comentario del caso oro U7 actualizado para reflejar que ese patch (antes construido "a mano" como recordatorio del gotcha) es ahora exactamente lo que produce `buildTimePatches` en el navegador. Manual de usuario: una línea en el generador y en el barrido temporal ("NOTTS e ITSO se sincronizan automáticamente"). Suite completa (Python + node) en verde antes y después. | Aprobada | A / S |
| F9 | inp-conf + analyzer | Análisis de contribución por cadenas (ACAB+CHAINS): pipeline N runs monoisotópicos + tapes 22/24 (IWP=3/IMTX=1) + N runs CHAINS (IFLAG=2); tablas R_i=A_i/A_ref y Y_z_i=R_i·X_z_i con control Σ R_i≈1; diagrama de cadena seleccionada. Nueva sección sobre fontanería de barridos, manifest propio. **Fases 0-3 ✅ (2026-07-26):** ejecutado fase a fase (`runbook_F9_analisis_cadenas.md`), 4 commits (`inp-conf:` ×2 Fase 0, `analyzer:` Fase 1, `inp-conf:` ×2 Fases 2-3). Fases 0-1: baseline empírico (fixtures `tape22`/`tape24` congelados, delta IWP 1→3/IMTX 0→1 confirmado por diff; conversión de unidades XCOMP=C_i×1e-24 confirmada) y parsers/códec en el analyzer (`leer_concentraciones_iniciales`, `nombre_a_zzaaas`, `leer_output_chains`). Fase 2 (generación): `chains_analysis.py` genera `tape22/`+`tape24/` (patch Bloque #11 sobre la referencia sin modificar) y un `iso_<isótopo>/`+`chains_<isótopo>/` por isótopo (patch monoisotópico Bloque #5 + ajuste de NUCZO a 1, invariante del formato no explícito en el runbook original); `chains_inventory.py` como fragmento sincronizado con el analyzer. Fase 3 (orquestación): `runner.py` (sincronizado en ambos configuradores) extendido con redirección stdin/stdout opcional en el paso 'run', necesaria para `chains.exe < input_chain.txt > output_chain.txt`; pipeline `tape22`→`tape24`→N×[ACAB monoisotópico, copiar tapes, CHAINS] vía `/api/chains-analysis/run`; el run IMTX=1 termina sin fort.6 y es éxito (verificado con ejecutables falsos, patrón D1). UI completa (pestaña "Análisis de cadenas") verificada en navegador real (Playwright): carga de inventario, selección de isótopos, previsualizar/generar, panel de ejecución en vivo. Suite completa (830 tests) en verde antes y después de cada fase. **Fases 4-5 ✅ (2026-07-26):** 1 commit `analyzer:` + 1 commit `suite:`. Fase 4 (tablas, analyzer): `fort_analyzer.calcular_analisis_cadenas(root, t_h, manifest)` — lee `chains_manifest.json` + el `fort.6` de la referencia (fuera de `root`) + el `fort.6` monoisotópico y el `output_chain.txt` de cada isótopo; R_i=A_i(IFINAL,t\*)/A_ref(IFINAL,t\*) con t\* por defecto el t_pico de la referencia (selector manual disponible); X_z_i=P_z_i/100 de CHAINS; Y_z_i=R_i·X_z_i. Tabla 1 con fila Σ R_i y nota de cobertura (completa/parcial, comparando contra `leer_concentraciones_iniciales` de la referencia); tabla 2 ordenada por Y_z_i descendente con NMAX/PCNT visibles. Endpoint dedicado `POST /api/chains_report` (sin caché propia, independiente de `/api/analyze`). Fase 5 (diagrama+cierre): `construir_diagrama_cadena` (nodos con T½ de DECAY.dat — "estable"/"T½ desconocido" como casos distintos —, aristas con proceso+XSEC/DELTA), embebido en cada fila de tabla 2, sin endpoint propio; grafo fusionado (Fig. 1 del paper) queda fuera de alcance v1 (F9b). Pestaña nueva "Análisis de cadenas" en el Fort Analyzer (independiente de la carpeta de simulaciones de arriba — único tab con su propio campo de carpeta), i18n es/en completa (parity verificada), diagrama lineal interactivo (clic en una fila de tabla 2), exportación CSV de ambas tablas. Manuales de usuario de ambas apps ampliados (nueva sección en cada uno). Tests oro: fixture **totalmente sintético** `tests/fixtures/chains_synthetic/` (2 isótopos ficticios FE56/MN55→CO57, con PROCEDENCIA.md documentando R_i/Σ R_i/X_z_i/Y_z_i verificados a mano: Σ R_i=1.00 con cobertura completa, tabla2 de 3 filas 0.580/0.336/0.084) en `test_chains.py` + `test_api.py::test_chains_report`; diagrama de cadena verificado contra el fixture REAL de Fase 1 (Te130→I131, T½ reales de DECAY.dat), sin fixture nuevo. **Verificación humana (tablón):** con el análisis sintético y cobertura completa, Σ R_i = 1.00 exacto (control de linealidad de Bateman, construido por diseño del fixture — la firma real sobre un caso físico queda pendiente de ejecutar el pipeline completo con `acab.exe`/`chains.exe` reales, fuera del alcance de esta sesión sin esos binarios). Sin acceso a navegador real (Playwright) en esta sesión: verificado en su lugar contra un servidor Flask real levantado en local (HTML de la pestaña, endpoint `/api/chains_report` con el caso sintético) y `node --check` sobre `app.js`; pendiente la verificación visual completa en un navegador. Suite completa (883 tests) en verde antes y después. **F9c (hotfix) ✅ (2026-07-26):** primera ejecución real del pipeline completo con `acab.exe`/`chains.exe` reales (la firma pendiente de Fases 4-5): confirmó por diagnóstico empírico en `run.log` ("forrtl: severe (24): end-of-file during read, unit 122, REACTIONS.dat", chains.f línea 50) que las carpetas `chains_*` se sembraban sin `REACTIONS.dat` ni `DECAY.dat` y chains.exe abortaba al arrancar; copiándolos a mano, el output se generaba correcto. Cuatro arreglos en `chains_analysis.py`: (1) sembrado de `chains_<isótopo>/` con `REACTIONS.dat`/`DECAY.dat` (`CHAINS_SEED_DATA_FILES`); (2) `_clean_chains_dir_before_run` limpia restos de un intento fallido (`output_chain.txt`/`fort.23`/`fort.31`) y re-siembra REACTIONS.dat/DECAY.dat sin condiciones antes de cada (re)lanzamiento, para que una carpeta generada ANTES de este hotfix se autorrepare al reejecutar; (3) sembrado de tape22/tape24/iso_* excluye fort.17/22/23/24 obsoletos de la referencia (`_CHAINS_BASE_EXCLUDE_EXTRA`, patrón C4); (4) UX de fallo en `runner.py`+`app.py`+`chains_sweep.js`: `status()` rastrea el cwd real del paso 'run' en curso (`_current_step_cwd`, no solo el workdir de nivel job), endpoint nuevo `GET /api/run/log?workdir=` (sincronizado en ambos configuradores) y la fila de estado traduce `step_index` vía `pipeline_steps` (antes "ACAB/CHAINS" ambiguo para cualquier paso 'run') con botón "Ver run.log" por fila fallida. Lección de método explícita: los ejecutables falsos de los tests validaban la redirección stdin/stdout pero NO el contrato de ficheros de entrada, y por eso este fallo atravesó la suite en verde — el chains.exe falso ahora comprueba REACTIONS.dat/DECAY.dat no vacíos (aborta con mensaje a stderr como el forrtl real si faltan) y el acab.exe falso, sus propios DECAY.dat/XSECTION.dat; test de regresión nuevo ("carpeta chains sin REACTIONS.dat → sub-paso CHAINS reportado y run.log visible en `chains_<isótopo>/`") que antes pasaría en verde y ahora no. 2 commits (`inp-conf:` + `suite:`). Suite completa (902 tests, `run_all_tests.ps1`) en verde antes y después. **F9d (hotfix) ✅ (2026-07-26):** dos casos borde de esa misma primera ejecución real (11 isótopos del blanco TeO₂: 8 de Te + O16/O17/O18), 1 commit `analyzer:`. (1) `leer_output_chains` lanzaba `ValueError: Campo no encontrado... NCHAIN` contra el `output_chain.txt` de O16/O17/O18 (idéntica forma entre los tres, solo difiere INITIAL): cuando CHAINS no encuentra ningún camino INITIAL→IFINAL en ≤ NMAX pasos (aquí, ningún isótopo de oxígeno decae a yodo) el fichero no tiene NCHAIN/NCH/PTOT ni bloques de cadena, solo la cabecera y el literal `THERE ARE NO PATHWAYS FOR FORMATION OF NUCLIDE IFINAL`; detectado por ese literal, devuelve `nchain=nch=0`/`ptot=0.0`/`cadenas=[]` sin lanzar. Fixture congelado `tests/fixtures/chains/output_chain_no_pathways_O16.txt` (ver su `PROCEDENCIA.md`, SHA256 verificado). (2) `calcular_analisis_cadenas` ya no bloqueaba el informe entero si el `output_chain.txt` de UN isótopo era ilegible o estaba ausente (bug de diseño, no ejercitado hasta esta primera ejecución real): ahora degrada por isótopo — la fila de `tabla1` conserva su R_i (dato físico válido con independencia de CHAINS) con `nota_cadenas="salida de CHAINS ilegible/sin cadenas"`, solo sus filas de `tabla2` se omiten; el caso "legible pero sin cadenas" (O16/O17/O18 tras el fix de (1)) no lleva nota — R_i≈0 se muestra igual (coherencia física) y tabla2 sin filas de ese isótopo. Columna "Cadenas" nueva en la tabla 1 de la pestaña (i18n es/en), export CSV actualizado. (3) Ruido de tapes: `descubrir_simulaciones` excluye por nombre `tape22`/`tape24` cuando la carpeta raíz analizada contiene `chains_manifest.json` (esos runs IWP=3/IMTX=1 no tienen sección NUMBER OF ATOMS por diseño, su rol ya está en el manifest) — evita el aviso "No se encontró NUMBER OF ATOMS" al analizar esa carpeta desde la pestaña normal de "Simulaciones". Tests oro nuevos en `test_chains.py` (fixture sin cadenas, output corrupto/ausente con copia temporal de `chains_synthetic/`, caso sin-cadenas-no-es-error), `test_api.py::test_chains_report` (mismo caso corrupto vía endpoint) y `test_fort_analyzer.py::test_descubrir_simulaciones_excluye_tapes_con_chains_manifest`. Suite completa (929 tests, `run_all_tests.ps1`) en verde antes y después; sin acceso a navegador real (Playwright) en esta sesión, verificado con `node --check` sobre `app.js` y el fixture real localizado en disco (`chains_O16`/`O17`/`O18` de la carpeta de análisis real). | Aprobada | A / L |
| B1 | analyzer | Espectro gamma de la muestra desde PHOTON.dat (librería genérica de líneas gamma discretas de ACAB, ya no solo el ¹³¹I hardcodeado). **✅ (2026-07-21):** ejecutado fase a fase (`runbook_B1_espectro_gamma.md`), 3 commits `analyzer:`. **Fase 1** — `leer_photon_dat(filepath) -> {acab_key: [[E_keV, intensidad_pct], ...]}`: bloques por nucleido (Z/símbolo+A[M]/nº de líneas + pares E[MeV]/intensidad, CRLF/LF tolerado, última fila parcial si nº no es múltiplo de 3), energías convertidas a keV. Fixture congelado `tests/fixtures/ref_sim/PHOTON_extract.dat` (16 nucleidos Te→I→Xe, CRLF byte a byte verificado con `git hash-object`). **Fase 2** — `calcular_espectro_gamma(sim, t_h, libreria)`: combina el inventario de enfriamiento en el timestep real más cercano con la librería, tasa = A(t)×intensidad/100 en fotones/(s·cm³) (conversión a fotones/(s·g) delegada al frontend, como en F1); nucleidos con actividad pero sin entrada en la librería → `nucleidos_sin_lineas`, nunca rompen el resto. **Fase 3** — endpoint `POST /api/espectro_gamma` (PHOTON.dat autodescubierto junto al fort.6 como DECAY.dat, override en caliente vía la propia pestaña, variable `ACAB_PHOTON_DAT` como último recurso); pestaña nueva "Espectro gamma" (independiente del isótopo seleccionado): selector de simulación/instante, filtros de energía y tasa mínima (`static/js/espectro_gamma_utils.js`, puro), stick plot Plotly coloreado por nucleido, tabla exportable a CSV, lista colapsable de nucleidos sin líneas. i18n es/en completa. **Verificación humana (firma numérica):** en t=4,5 h de ref_sim (enfriamiento tardío, ¹³¹I casi puro) la tabla de la pestaña da tasa(364,49 keV)=1.339e+4 fotones/(s·cm³) = A(¹³¹I,4,5h)=16490 Bq/cm³ × 0,812, recalculado a mano y congelado en `test_photon.py`/`test_api.py`. Verificación cruzada opcional (tabla `GROUP PHOTON RELEASE RATES` del fort.6): descartada — la tabla está uniformemente a cero en las 4 apariciones del fort.6 de ref_sim (esta simulación no calculó esa magnitud), documentado aquí en vez de forzar una comparación sin datos. Verificado en navegador real (Playwright): carga de la librería desde la propia pestaña, instante temprano (Te131/Te131m dominantes) y tardío (364 keV dominante) coherentes con la física del problema, filtro de tasa mínima limpia la vista de 11 a 3 nucleidos (I-132 desaparece), exportación CSV, cambio de idioma sin perder los filtros aplicados, sin errores de consola. Suite completa (Python + node) en verde antes y después de cada fase. | Aprobada | A / M |

| B1b | analyzer | Pulido de la pestaña "Espectro gamma" tras primer uso real. **✅ (2026-07-22):** commit `analyzer:`. (1) `POST /api/browse-file` (variante de `/api/browse-folder` con `filedialog.askopenfilename`) + botón explorador junto al campo manual de ruta de PHOTON.dat; última ruta cargada con éxito recordada en `localStorage` (`fort-analyzer-photon-path`) y reintentada automáticamente en silencio al abrir la pestaña (solo si el servidor no autodescubrió ya una librería junto al fort.6; sin toast si la ruta ya no existe). (2) Investigado primero si había bug de render: NO lo había — los datos y el autorange de Plotly ya eran correctos (línea de 364 keV = 1.340e+4, verificado contra la tabla en t=3,750h), el problema era la legibilidad por ~30 décadas de rango dinámico de las líneas más débiles. `ACABEspectroGamma.umbralPorDefecto` (máximo del instante/1e6) puebla el campo de tasa mínima con un valor efectivo editable; deja de recalcularse en cuanto el usuario toca el campo a mano (tecleando "0" se desactiva el filtro explícitamente). (3) `construirTrazasStickTopN`: leyenda acotada a los 8 nucleidos de mayor tasa TOTAL, resto agrupado en una única traza "otros" con color neutro (hover por punto vía `customdata`, conserva el nucleido real). i18n es/en completa. Tests nuevos en `test_espectro_gamma_utils.js` (umbral por defecto con el caso oro real de t=3,750h, `topNNucleidos` por tasa total, agrupación top-N/otros, customdata por punto). Verificado en navegador real (Playwright): vista inicial en t=3,750h legible SIN tocar ningún filtro (leyenda de 3 nucleidos, rango Y de ~5 décadas en vez de ~30), auto-carga desde `localStorage` en una sesión nueva sin ninguna interacción manual, "0 = sin filtro" restaura las 50 filas, sin errores de consola. Suite completa (Python + node) en verde antes y después. | Aprobada | A / S |

## Deuda técnica

| # | Comp. | Ítem | Prio/Esf |
|---|---|---|---|
| D1 | inp-conf | Ejecutables falsos de los tests de P4 son `.bat` (solo Windows); migrar al patrón de la suite: falsos Python multiplataforma (R1). **✅ (2026-07-16):** commit `inp-conf:`. `test_run_batch_endpoint.py` (`TestSpectrumPipelineEndToEnd`, el único con `.bat`): `_write_fake_launcher` escribe en Windows un `.bat` lanzador mínimo que delega en un script Python real y propaga su código de salida; en POSIX el propio script Python con shebang y permiso de ejecución, sin lanzador aparte. El runner no cambia (sigue invocando el ejecutable configurado sin argumentos, con cwd); mismo comportamiento de los fakes (mismas salidas, mismos códigos de retorno), suite completa en verde sin modificar la lógica de aserciones. Sin verificación manual en Linux real en esta sesión (solo Windows disponible). | B / S |

## Ideas registradas sin compromiso (del análisis inicial, no ejecutadas)

- collaps: gráfica del espectro FT en el propio configurador (la superposición ya
  existe en la pestaña Barrido del INP configurator; valorar si sigue aportando).
- collaps: presets de estructuras de grupos estándar para la tarjeta 7.
- Barridos: producto cartesiano (v2 declarada en el runbook v2) y combinación
  espectro × flujo (XNORM quedó libre a propósito, decisión D1 del espectral).
- Suite: fusión de las apps en una sola Flask con blueprints (post-TFG).
